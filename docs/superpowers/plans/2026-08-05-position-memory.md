# 阅读位置记忆功能 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让批注视图记住每个文件的阅读位置（PDF 页码 / Markdown 滚动位置），退出并重开后恢复，且长期持久化、随库同步。

**Architecture:** 在插件 `data.json` 的 settings 中维护 `savedPositions: Record<filePath, {kind:"pdf",page}|{kind:"markdown",scrollTop}>` 位置记忆表。滚动时防抖记录，`onClose()` 强制落盘；`render()` 完成后根据文件类型调用 `goToPdfPage()` 或 `scrollTo()` 恢复。

**Tech Stack:** TypeScript、Obsidian API（`Plugin.saveData/loadData`、`FileView`）、esbuild 打包。

## Global Constraints

- 唯一外部运行时依赖：`obsidian`（源码已用 `loadPdfJs` 处理 PDF）。不得新增运行时依赖。
- 不修改 PDF / Markdown 源文件本身。
- 沿用仓库既有命名约定：类型为 PascalCase，私有成员 `camelCase`，字符串文案使用简体中文。
- 保持 `main.js` 为 esbuild 产物（构建后约 250KB）。
- 项目 GPL-3.0，注释文案沿用中文。
- node ≥ 20（原 `package.json` 声明，本机 v24.14.0 满足），npm 11。`package.json` 中 `author` 保留 `"ChatGPT"`、`license` 保留 `"GPL-3.0-or-later"` 以对齐原版。

---

### Task 1: 重建构建环境（package.json + esbuild/tsconfig）

**Files:**
- Create: `package.json`（含 build/dev 脚本与 devDependencies）
- Create: `esbuild.config.mjs`
- Create: `tsconfig.json`
- Test: 构建产物 `main.js`

**Interfaces:**
- Consumes: 仓库原有 `src/**`、`main.js`
- Produces: `npm run build` 可用的构建脚本；`tsc -noEmit` 类型检查脚本。

**背景**：源仓库缺 `package.json`/`esbuild.config.mjs`/`tsconfig.json`（git 历史中 `package.json` 于提交 `3e8d12c` 被删除，esbuild/tsconfig 从未存在于历史中）。需按 Obsidian 社区标准重建，以便从 `src/` 重新编译 `main.js`。

- [ ] **Step 1: 验证当前 main.js 可被构建替换（先保存基线）**

```bash
Copy-Item main.js "$env:TEMP\mia-main-backup.js"
```
Expected: 备份成功。

- [ ] **Step 2: 创建 package.json**

```json
{
  "name": "obsidian-mobile-ink-annotation",
  "version": "1.0.5",
  "description": "Mobile-first handwritten annotation plugin for Obsidian.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
  },
  "keywords": ["obsidian", "plugin", "ink", "annotation", "pencil", "mobile"],
  "author": "ChatGPT",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "@types/node": "^20.11.30",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.20.2",
    "obsidian": "latest",
    "pdfjs-dist": "^5.7.284",
    "typescript": "^5.4.5"
  }
}
```

- [ ] **Step 3: 创建 esbuild.config.mjs**

```js
import esbuild from "esbuild";
import process from "process";
import builtins from "builtin-modules";

const banner = `/*
THIS IS A GENERATED/BUNDLED FILE BY ESBUILD
If you want to view the source, please visit the plugin repository.
*/
`;

const prod = process.argv[2] === "production";

const context = await esbuild.context({
  banner: { js: banner },
  entryPoints: ["src/main.ts"],
  bundle: true,
  external: [
    "obsidian",
    "electron",
    "@codemirror/autocomplete",
    "@codemirror/collab",
    "@codemirror/commands",
    "@codemirror/language",
    "@codemirror/lint",
    "@codemirror/search",
    "@codemirror/state",
    "@codemirror/view",
    "@lezer/common",
    "@lezer/highlight",
    "@lezer/lr",
    ...builtins,
  ],
  format: "cjs",
  target: "es2018",
  logLevel: "info",
  sourcemap: prod ? false : "inline",
  treeShaking: true,
  outfile: "main.js",
});

if (prod) {
  await context.rebuild();
  process.exit(0);
} else {
  await context.watch();
}
```

- [ ] **Step 4: 创建 tsconfig.json**

```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "inlineSourceMap": true,
    "inlineSources": true,
    "module": "ESNext",
    "target": "ES2018",
    "allowJs": true,
    "noImplicitAny": true,
    "moduleResolution": "node",
    "importHelpers": true,
    "isolatedModules": true,
    "strictNullChecks": true,
    "skipLibCheck": true,
    "lib": ["DOM", "ES5", "ES6", "ES7", "ES2020", "ES2018.AsyncIterable", "DOM.Iterable"]
  },
  "include": ["src/**/*.ts"]
}
```

- [ ] **Step 5: 安装依赖**

```bash
npm install
```
Expected: 成功安装，生成 `node_modules` 与 `package-lock.json`。

- [ ] **Step 6: 构建，验证产物已生成且与基线逻辑一致（不含功能改动时 diff 应仅在 banner/空格）**

```bash
npm run build
```
Expected: `tsc -noEmit` 通过（无类型错误），`main.js` 重新生成，退出码 0。

- [ ] **Step 7: 提交**

```bash
git add package.json package-lock.json esbuild.config.mjs tsconfig.json
git commit -m "chore: rebuild esbuild/tsc build tooling"
```

---

### Task 2: 扩展 settings 类型与默认值

**Files:**
- Modify: `src/main.ts:6-12`

**Interfaces:**
- Consumes: 无（仅修改类型）
- Produces: `SavedFilePosition` 类型；`settings.savedPositions: Record<string, SavedFilePosition>` 供 Task 3 读取。

- [ ] **Step 1: 修改设置类型与默认值**

将 `src/main.ts` 第 6-12 行：

```ts
type MobileInkAnnotationSettings = {
  openPdfWithAnnotationByDefault: boolean;
};

const DEFAULT_SETTINGS: MobileInkAnnotationSettings = {
  openPdfWithAnnotationByDefault: false
};
```

替换为：

```ts
export type SavedFilePosition =
  | { kind: "pdf"; page: number }
  | { kind: "markdown"; scrollTop: number };

type MobileInkAnnotationSettings = {
  openPdfWithAnnotationByDefault: boolean;
  savedPositions: Record<string, SavedFilePosition>;
};

const DEFAULT_SETTINGS: MobileInkAnnotationSettings = {
  openPdfWithAnnotationByDefault: false,
  savedPositions: {}
};
```

- [ ] **Step 2: 构建验证类型正确**

```bash
npm run build
```
Expected: tsc 通过，退出码 0。（新增字段未使用不报错。）

- [ ] **Step 3: 提交**

```bash
git add src/main.ts
git commit -m "feat: add savedPositions field to plugin settings"
```

---

### Task 3: 在 AnnotationView 中记录并恢复阅读位置

**Files:**
- Modify: `src/views/AnnotationView.ts:2207-2212`（onAnnotationScroll 加入防抖记录）
- Modify: `src/views/AnnotationView.ts:479-491`（onClose 强制落盘）
- Modify: `src/views/AnnotationView.ts:730-733`（render 完成后恢复）
- Modify: `src/mai.ts` 无需改（settings 已在 Task 2）

**Interfaces:**
- Consumes: `settings.savedPositions`（Task 2）、`getCurrentPageNumber()`（PdfPageNavigatorManager）、`this.scrollEl.scrollTop`、`goToPdfPage(page)`、`this.isPdfPath(path)`
- Produces: 位置记录持久化到 settings；重开同一文件时恢复。

**Step 1-2（记录）需读取 `this.sourcePath`、判断是否 PDF。注意 `sourcePath` 为成员变量，`scrollEl` 为成员变量。**

- [ ] **Step 1: 在 onAnnotationScroll 中加入防抖记录**

将 `src/views/AnnotationView.ts:2207`：

```ts
private onAnnotationScroll = (): void => {
  if (this.pdfTextDragSelection || this.pdfPendingTextSelection || this.activePdfTextTouchId !== null) return;

  this.hidePdfTextMenu();
  this.hidePdfAnnotationMenu();
};
```

替换为：

```ts
private onAnnotationScroll = (): void => {
  if (this.pdfTextDragSelection || this.pdfPendingTextSelection || this.activePdfTextTouchId !== null) return;

  this.hidePdfTextMenu();
  this.hidePdfAnnotationMenu();
  this.scheduleSavePosition();
};
```

并新增成员方法（追加到 `onClose` 之后，即 `AnnotationView.ts:491` 之后）：

```ts
private positionSaveTimer: number | null = null;

private scheduleSavePosition(): void {
  if (this.positionSaveTimer !== null) {
    window.clearTimeout(this.positionSaveTimer);
  }
  this.positionSaveTimer = window.setTimeout(() => {
    this.positionSaveTimer = null;
    void this.savePosition();
  }, 800);
}

private async savePosition(): Promise<void> {
  const path = this.sourcePath;
  if (!path || !this.scrollEl) return;

  const pos: SavedFilePosition = this.isPdfPath(path)
    ? { kind: "pdf", page: this.pdfPageNavigator.getCurrentPageNumber() }
    : { kind: "markdown", scrollTop: this.scrollEl.scrollTop };

  if (!this.plugin.settings.savedPositions) {
    this.plugin.settings.savedPositions = {};
  }
  this.plugin.settings.savedPositions[path] = pos;
  await this.plugin.saveSettings();
}
```

- [ ] **Step 2: 在 onClose 中强制落盘一次（清除防抖计时器）**

将 `src/views/AnnotationView.ts:479` 的 `onClose` 开头：

```ts
async onClose(): Promise<void> {
  await this.flushCurrentWork();
```

替换为：

```ts
async onClose(): Promise<void> {
  if (this.positionSaveTimer !== null) {
    window.clearTimeout(this.positionSaveTimer);
    this.positionSaveTimer = null;
  }
  await this.savePosition();
  await this.flushCurrentWork();
```

- [ ] **Step 3: 在 render() 完成后恢复位置**

将 `src/views/AnnotationView.ts:730-733`：

```ts
      this.refreshToolbarState();
    } finally {
      this.rendering = false;
    }
  }
```

替换为：

```ts
      this.restoreSavedPosition();
      this.refreshToolbarState();
    } finally {
      this.rendering = false;
    }
  }
```

并新增方法（追加到 `onClose` 之后）：

```ts
private restoreSavedPosition(): void {
  const path = this.sourcePath;
  if (!path || !this.scrollEl) return;
  const saved = this.plugin.settings.savedPositions?.[path];
  if (!saved) return;

  if (saved.kind === "pdf" && this.isPdfPath(path)) {
    this.goToPdfPage(saved.page);
  } else if (saved.kind === "markdown" && !this.isPdfPath(path)) {
    this.scrollEl.scrollTo(0, saved.scrollTop);
  }
}
```

- [ ] **Step 4: 在文件顶部导入 SavedFilePosition 类型**

在 `src/views/AnnotationView.ts` 顶部 import 区（`AnnotationView.ts:1` 之后）加入类型导入。查到 `export type { AnnotationViewState } from "./annotationTypes";`（第 86 行）附近有其它类型导入。更稳妥的做法是直接从 `../main` 导入：

将第 2 行：

```ts
import type MobileInkAnnotationPlugin from "../main";
```

替换为：

```ts
import type MobileInkAnnotationPlugin, { SavedFilePosition } from "../main";
```

- [ ] **Step 5: 构建验证无类型错误**

```bash
npm run build
```
Expected: `tsc -noEmit` 通过，退出码 0。若报 `pdfPageNavigator` 或 `goToPdfPage` 未定义，确认其均为私有成员且在类中已存在（已在 grep 中确认：`getCurrentPageNumber` @3725、`goToPdfPage` @3546）。

- [ ] **Step 6: 提交**

```bash
git add src/views/AnnotationView.ts
git commit -m "feat: remember and restore reading position per file"
```

---

### Task 4: 手动功能验证

**Files:**
- Test: 构建产物 `main.js`

**Interfaces:**
- Consumes: Task 1-3 的完整产物。

在 Obsidian 中进行真机验证（需要用户参与）：

- [ ] **Step 1: 复制插件到测试 vault**

将整个仓库目录（含 `main.js`、`manifest.json`、`styles.css`）复制到测试 vault：
```text
<测试库>/.obsidian/plugins/mobile-ink-annotation-basic/
```
然后在 Obsidian 设置 → 第三方插件 中开启该插件（若版本号相同需先禁用重开或改期 version 再加载）。

- [ ] **Step 2: 验证 PDF 位置记忆**

打开一个 PDF → 进入批注视图 → 翻到第 5 页 → 关闭批注视图 → 重新打开同一 PDF。
Expected: 自动恢复/停留在第 5 页。

- [ ] **Step 3: 验证 Markdown 位置记忆**

打开一个 Markdown → 进入批注视图 → 滚动到中下部 → 关闭 → 重开。
Expected: 恢复到原滚动位置。

- [ ] **Step 4: 验证持久化**

完全重启 Obsidian → 重开同一 PDF/Markdown。
Expected: 位置仍恢复（数据在 `data.json` 的 `savedPositions` 中）。

- [ ] **Step 5: 验证从未批注的文件**

打开一个从未打开过的文件。
Expected: 从顶部/第 1 页开始，无报错。

- [ ] **Step 6: 记录并提交测试结果说明**

确认 `data.json` 中出现形如 `"savedPositions":{"<path>":{"kind":"pdf","page":5}}` 的数据。

---

## 自审

**Spec 覆盖**：
- PDF 页码记录/恢复 → Task 3 Step 1/3
- Markdown 滚动记录/恢复 → Task 3 Step 1/3
- 长期持久化 → Task 2 + Task 3 saveSettings/onClose 落盘
- 跨端同步 → 数据存于 data.json（库内），由 Obsidian 同步天然覆盖
- 文件名变更退化 → restore 时 `savedPositions[path]` 不存在则跳过，自然退化

**占位符扫描**：无 TBD/实现后填充等。每个代码块均给出完整实现。

**类型一致性**：`SavedFilePosition` union 类型在 Task 2 定义 `{kind:"pdf",page}|{kind:"markdown",scrollTop}`，Task 3 中 `savePosition`/`restoreSavedPosition` 与之一致；`settings.savedPositions` 字段名全篇一致。